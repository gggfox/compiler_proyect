program MyRlike; 

function int stuff(){

}
    // existe un error donde se pueden guardar tipos
    // distintos en cualquier clase

main(){
    vars {
        int: A[10],B;
    }
    B = 2;
    // 20
    A[0] = 5 + 6 * (4 - 2) + 3;
    // 100
    A[1] = 5 * 6 + 8 * 9 - 2;
    // 4050
    A[2] = 5 * (6 * (7 + 8)) * 9;
    // 4170
    A[3] = A[0] + A[1] + A[2];
    // 
    write(A[3],"\n");
    
}

    // # (-, 4, 2, t1)
    // # (*, 6, t1, t2)
    // # (+, 5, t2, t3)
    // # (=,t3, None, A)