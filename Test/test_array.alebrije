program MyRlike; 
vars{
    int: W[20];
}

function int stuff(){

}
    // existe un error donde se pueden guardar tipos
    // distintos en cualquier clase

main(){
    vars {
        int: A[10],B;
    }
    B = 2;
    // 20
    A[0] = 9 + 6 * (4 - 2) - 1;
    // 100
    A[1] = 5 * 6 + 8 * 9 - 2;
    // 4050
    A[B] = 5 * (6 * (7 + 8)) * 9;
    // 4170
    A[3+1] = A[0] + A[1] + A[B];
    write(A[3+1],"\n");
    
}