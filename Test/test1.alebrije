program MyRlike; 

vars {  
    int : i,j;
}

####DELETE
function int calcula(int: y){
    vars {
        int: x,y;
    }
    while(x < 11) do{
        y = y * arreglo[x];
        x = x + 1;
        write(arreglo[x]);
    }
    write("acumulado",y);
}
#########


main(){
    vars {
        float: x,y,z;
    }
    if(j == 1){
        return(j);
    }else{
        return(j*fact(j - 1));
    }
}