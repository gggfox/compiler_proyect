program MyRlike; 

function int stuff(){

}
    // existe un error donde se pueden guardar tipos
    // distintos en cualquier clase

main(){
    vars {
        int: A,B,C;
        bool: D,E,F;
        float: G;
    }
    // 17
    A = 5 + 6 * (4 - 2);
    // 102 
    B = 5 * 6 + 8 * 9;
    // 4050
    C = 5 * (6 * (7 + 8)) * 9;
    // True
    D = 1+1 == 1;
    // True
    E = (4 + 3 >= 5) and (5 <= 6);

    F = True;

    A *= 2;
    A -= 4;
}

    // # (-, 4, 2, t1)
    // # (*, 6, t1, t2)
    // # (+, 5, t2, t3)
    // # (=,t3, None, A)